aside.tools_panel(ng-class='{visible: conversationDetailView == true}')
    header.app_header
      button.left.icon.btn(ng-click='toggleToolPanel()')
        i.icon-cancel
      h1 Collaboration Tools
    .scroller_wrapper.with_header_padding
      .scroller
        dl.tools_list
          dt importance:
          dd(ng-bind='viewConversation.importance')
          dt tags:
          dd
            ul.tag_list.inline
              li.inline.tag(ng-repeat='tag in viewConversation.tags')
                span(ng-bind='tag')
                span.close_btn(ng-click='removeTag(tag, viewConversation.tags)') x
            form(ng-submit='addTopicTag(tag_to_submit, viewConversation)', name='topic_tag')
              input(type='text', ng-model='tag_to_submit')
              button.right.btn(type='submit') save tag
          dt tagged users:
          dd
            ul.tag_list.inline
              li.inline.tag(ng-repeat='user in viewConversation.tagged_users', ng-animate='\'collapse\'')
                span(ng-bind='user')
                span.close_btn(ng-click='removeTag(user, viewConversation.tagged_users)') x
            form(ng-submit='addUserTag(user_to_submit, viewConversation)', name='user_tag')
              input(type='text', ng-model='user_to_submit')
              button.right.btn(type='submit') save tag
          dt Notes:
          dd
            ul.notes
              li.note(ng-repeat='comment in viewConversation.comments', ng-animate='\'collapse\'')
                span(ng-bind='comment.text')
                br
                small
                  | -
                  span(ng-bind='comment.author')
            form(ng-submit='addComment(comment_text, viewConversation.comments)', name='comment_form')
              input(type='text', ng-model='comment_text')
              button.right.btn(type='submit') save comment
